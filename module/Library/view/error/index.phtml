<?php
declare(strict_types=1);
$this->layout('layout/library-layout');
?>
<div style="display:flex;flex-direction:column;align-items:center;justify-content:center;min-height:60vh;text-align:center;padding:40px 24px">
    <div style="width:72px;height:72px;background:rgba(239,68,68,.12);border:1px solid rgba(239,68,68,.25);border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:32px;color:#ef4444;margin:0 auto 20px">
        <i class="bi bi-exclamation-triangle"></i>
    </div>
    <div style="font-size:22px;font-weight:700;color:var(--text-primary);margin-bottom:10px">Đã xảy ra lỗi</div>
    <p style="color:var(--text-muted);font-size:14px;max-width:460px;margin:0 auto 24px">
        <?= $this->escapeHtml((string) ($this->message ?? 'Hệ thống không thể xử lý yêu cầu này.')) ?>
    </p>
    <?php if (! empty($this->display_exceptions) && isset($this->exception) && $this->exception instanceof \Throwable) : ?>
    <div class="card" style="max-width:720px;width:100%;text-align:left;margin-bottom:20px">
        <div class="card-header">
            <div class="card-title"><i class="bi bi-bug" style="color:#ef4444;margin-right:8px"></i>Thông tin kỹ thuật</div>
        </div>
        <div class="card-body" style="font-size:13px;display:grid;gap:8px">
            <div><span style="color:var(--text-muted)">Loại lỗi:</span> <code style="color:#ec4899;background:var(--bg-secondary);padding:2px 6px;border-radius:4px"><?= $this->escapeHtml(get_class($this->exception)) ?></code></div>
            <div><span style="color:var(--text-muted)">Thông điệp:</span> <span style="color:var(--text-primary)"><?= $this->escapeHtml($this->exception->getMessage()) ?></span></div>
            <div><span style="color:var(--text-muted)">Vị trí:</span> <code style="color:var(--accent-orange);background:var(--bg-secondary);padding:2px 6px;border-radius:4px"><?= $this->escapeHtml($this->exception->getFile() . ':' . $this->exception->getLine()) ?></code></div>
        </div>
    </div>
    <?php endif; ?>
    <a href="<?= $this->url('home') ?>" class="btn btn-outline">
        <i class="bi bi-house"></i>Quay về trang chính
    </a>
</div>
