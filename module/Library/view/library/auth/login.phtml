<?php $this->headTitle('Đăng nhập');
$this->layout('layout/library-layout'); 

$publicDir = getcwd();
if (basename($publicDir) !== 'public') {
    $publicDir .= '/public';
}
$customLogoFile = null;
foreach (['logo.png','logo.jpg','logo.jpeg','logo.svg','logo.webp'] as $_lf) {
    if (file_exists($publicDir . '/img/uploads/' . $_lf)) {
        $customLogoFile = 'img/uploads/' . $_lf; break;
    }
}
?>
<div class="login-card">
    <a href="<?= $this->url('catalog') ?>" class="btn btn-outline btn-icon" style="position: absolute; top: 20px; left: 20px;" title="Quay lại trang chủ">
        <i class="bi bi-arrow-left"></i>
    </a>
    <div class="login-logo">
        <?php if ($customLogoFile): ?>
            <img src="<?= $this->basePath($customLogoFile) ?>?t=<?= filemtime($publicDir . '/' . $customLogoFile) ?>" alt="Logo">
        <?php else: ?>
            <i class="bi bi-bookshelf"></i>
        <?php endif; ?>
    </div>
    <div class="login-title">Thư Viện HDPE</div>
    <div class="login-sub">Hệ thống Quản lý Thư viện</div>
    <form method="POST" action="<?= $this->url('library/auth', ['action' => 'login']) ?>">
        <div class="form-group">
            <label class="form-label">Tên đăng nhập</label>
            <input type="text" name="username" class="form-control" placeholder="username"
                value="<?= htmlspecialchars((string) $form->get('username')->getValue()) ?>" autofocus required>
            <?php $msgs = $form->get('username')->getMessages();
            if ($msgs): ?>
                <div class="login-error-msg"><?= htmlspecialchars(implode(', ', $msgs)) ?></div>
            <?php endif; ?>
        </div>
        <div class="form-group">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
                <label class="form-label" style="margin-bottom: 0;">Mật khẩu</label>
                <a href="<?= $this->url('library/auth', ['action' => 'forgotPassword']) ?>" class="login-footer-link" style="font-size: 13px; font-weight: 500; text-decoration: none;">Quên mật khẩu?</a>
            </div>
            <div style="position: relative;">
                <input type="password" name="password" id="login-password" class="form-control" placeholder="••••••" required style="padding-right: 40px;">
                <button type="button" onclick="togglePasswordVisibility('login-password', this)" style="position: absolute; right: 12px; top: 50%; transform: translateY(-50%); background: none; border: none; color: var(--text-muted); cursor: pointer; padding: 0; display: flex; align-items: center;">
                    <i class="bi bi-eye"></i>
                </button>
            </div>
            <?php $msgs = $form->get('password')->getMessages();
            if ($msgs): ?>
                <div class="login-error-msg"><?= htmlspecialchars(implode(', ', $msgs)) ?></div>
            <?php endif; ?>
        </div>
        <?php /* CSRF Disabled
        <?= $this->formHidden($form->get('csrf')) ?>
        <?php $msgs = $form->get('csrf')->getMessages();
        if ($msgs): ?>
            <div class="login-error-msg-mb"><?= htmlspecialchars(implode(', ', $msgs)) ?></div>
        <?php endif; ?>
        */ ?>
        <button type="submit" class="btn btn-primary w-100 login-btn">
            <i class="bi bi-box-arrow-in-right"></i>Đăng nhập
        </button>

        <!-- Google Login (New Category) -->
        <div style="position: relative; text-align: center; margin: 20px 0 15px;">
            <hr style="border-top: 1px solid var(--border-color); opacity: 0.2;">
            <span style="position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); background: var(--bg-card); padding: 0 12px; font-size: 12px; color: var(--text-muted); text-transform: uppercase; letter-spacing: 1px;">Hoặc</span>
        </div>

        <a href="<?= $this->url('google-login') ?>" class="btn btn-outline w-100 d-flex align-items-center justify-content-center gap-2" style="height: 44px; font-weight: 600; border-color: rgba(255,255,255,0.15);">
            <img src="https://www.gstatic.com/images/branding/product/1x/gsa_512dp.png" width="18" height="18">
            Tiếp tục với Google
        </a>
    </form>
    <p class="login-footer-text">
        Chưa có tài khoản?
        <a href="<?= $this->url('library/auth', ['action' => 'register']) ?>" class="login-footer-link">Đăng ký</a>
    </p>
    <div class="login-copyright">
        © 2026 Bản quyền thuộc về <span class="login-footer-link">Thư Viện HDPE</span>
    </div>
</div>

<script>
function togglePasswordVisibility(inputId, btnEl) {
    const input = document.getElementById(inputId);
    const icon = btnEl.querySelector('i');
    if (input.type === 'password') {
        input.type = 'text';
        icon.classList.remove('bi-eye');
        icon.classList.add('bi-eye-slash');
    } else {
        input.type = 'password';
        icon.classList.remove('bi-eye-slash');
        icon.classList.add('bi-eye');
    }
}
</script>