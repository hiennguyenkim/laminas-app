<?php $this->headTitle('Đăng ký');
$this->layout('layout/library-layout'); 

$publicDir = getcwd();
if (basename($publicDir) !== 'public') {
    $publicDir .= '/public';
}
$customLogoFile = null;
foreach (['logo.png','logo.jpg','logo.jpeg','logo.svg','logo.webp'] as $_lf) {
    if (file_exists($publicDir . '/img/uploads/' . $_lf)) {
        $customLogoFile = 'img/uploads/' . $_lf; break;
    }
}
?>
<div class="login-card register-card">
    <a href="<?= $this->url('catalog') ?>" class="btn btn-outline btn-icon" style="position: absolute; top: 20px; left: 20px;" title="Quay lại trang chủ">
        <i class="bi bi-arrow-left"></i>
    </a>
    <div class="login-logo">
        <?php if ($customLogoFile): ?>
            <img src="<?= $this->basePath($customLogoFile) ?>?t=<?= filemtime($publicDir . '/' . $customLogoFile) ?>" alt="Logo">
        <?php else: ?>
            <i class="bi bi-bookshelf"></i>
        <?php endif; ?>
    </div>
    <div class="login-title">Đăng ký tài khoản</div>
    <div class="login-sub">Hệ thống Quản lý Thư viện</div>
    <form method="POST" action="<?= $this->url('library/auth', ['action' => 'register']) ?>">
        <div class="form-group mb-3">
            <label class="form-label">Họ và tên</label>
            <input type="text" name="full_name" class="form-control" placeholder="Nguyễn Văn A"
                   value="<?= htmlspecialchars((string) $form->get('full_name')->getValue()) ?>" required>
            <?php $messages = $form->get('full_name')->getMessages(); if ($messages) : ?>
                <div class="login-error-msg"><?= htmlspecialchars(implode(', ', $messages)) ?></div>
            <?php endif; ?>
        </div>
        <div class="form-group mb-3">
            <label class="form-label">Tên đăng nhập</label>
            <input type="text" name="username" class="form-control" placeholder="username"
                   value="<?= htmlspecialchars((string) $form->get('username')->getValue()) ?>" required>
            <?php $messages = $form->get('username')->getMessages(); if ($messages) : ?>
                <div class="login-error-msg"><?= htmlspecialchars(implode(', ', $messages)) ?></div>
            <?php endif; ?>
        </div>
        <div class="form-group mb-3">
            <label class="form-label">Email</label>
            <input type="email" name="email" class="form-control" placeholder="name@example.com"
                   value="<?= htmlspecialchars((string) $form->get('email')->getValue()) ?>" required>
            <?php $messages = $form->get('email')->getMessages(); if ($messages) : ?>
                <div class="login-error-msg"><?= htmlspecialchars(implode(', ', $messages)) ?></div>
            <?php endif; ?>
        </div>
        <div class="form-group mb-3">
            <label class="form-label">Mật khẩu</label>
            <div style="position: relative;">
                <input type="password" name="password" id="register-password" class="form-control" placeholder="Tối thiểu 8 ký tự, có in hoa & ký tự đặc biệt" required style="padding-right: 40px;">
                <button type="button" onclick="togglePasswordVisibility('register-password', this)" style="position: absolute; right: 12px; top: 50%; transform: translateY(-50%); background: none; border: none; color: var(--text-muted); cursor: pointer; padding: 0; display: flex; align-items: center;">
                    <i class="bi bi-eye"></i>
                </button>
            </div>
            <?php $messages = $form->get('password')->getMessages(); if ($messages) : ?>
                <div class="login-error-msg"><?= htmlspecialchars(implode(', ', $messages)) ?></div>
            <?php endif; ?>
            
            <!-- Password strength indicator & checklist -->
            <div style="margin-top: 10px; background: rgba(255,255,255,0.02); border: 1px solid var(--border-color); border-radius: 10px; padding: 12px;">
                <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 8px;">
                    <span style="font-size: 12px; font-weight: 600; color: var(--text-secondary);">Độ mạnh mật khẩu:</span>
                    <span id="strength-label" style="font-size: 11px; font-weight: 700; color: var(--text-muted);">Trống</span>
                </div>
                <div style="height: 6px; background: var(--border-color); border-radius: 3px; overflow: hidden; margin-bottom: 12px; display: flex;">
                    <div id="strength-bar" style="width: 0%; height: 100%; transition: all 0.3s ease; background: #ef4444;"></div>
                </div>
                <div style="display: flex; flex-direction: column; gap: 6px; font-size: 11px;">
                    <div id="req-length" style="display: flex; align-items: center; gap: 6px; color: var(--text-muted); transition: color 0.2s;">
                        <i class="bi bi-circle"></i> Tối thiểu 8 ký tự
                    </div>
                    <div id="req-uppercase" style="display: flex; align-items: center; gap: 6px; color: var(--text-muted); transition: color 0.2s;">
                        <i class="bi bi-circle"></i> Chứa ít nhất 1 chữ in hoa
                    </div>
                    <div id="req-special" style="display: flex; align-items: center; gap: 6px; color: var(--text-muted); transition: color 0.2s;">
                        <i class="bi bi-circle"></i> Chứa ít nhất 1 ký tự đặc biệt
                    </div>
                </div>
            </div>
        </div>
        <div class="form-group mb-4">
            <label class="form-label">Xác nhận mật khẩu</label>
            <div style="position: relative;">
                <input type="password" name="password_confirm" id="register-password-confirm" class="form-control" placeholder="Nhập lại mật khẩu" required style="padding-right: 40px;">
                <button type="button" onclick="togglePasswordVisibility('register-password-confirm', this)" style="position: absolute; right: 12px; top: 50%; transform: translateY(-50%); background: none; border: none; color: var(--text-muted); cursor: pointer; padding: 0; display: flex; align-items: center;">
                    <i class="bi bi-eye"></i>
                </button>
            </div>
            <?php $messages = $form->get('password_confirm')->getMessages(); if ($messages) : ?>
                <div class="login-error-msg"><?= htmlspecialchars(implode(', ', $messages)) ?></div>
            <?php endif; ?>
        </div>
        <?= $this->formHidden($form->get('csrf')) ?>
        <?php $messages = $form->get('csrf')->getMessages(); if ($messages) : ?>
            <div class="login-error-msg-mb"><?= htmlspecialchars(implode(', ', $messages)) ?></div>
        <?php endif; ?>
        <button type="submit" class="btn btn-primary w-100 login-btn">
            <i class="bi bi-check-circle"></i>Đăng ký
        </button>

        <!-- Google Login -->
        <div style="position: relative; text-align: center; margin: 20px 0;">
            <div style="position: absolute; top: 50%; left: 0; right: 0; height: 1px; background: rgba(255,255,255,0.1);"></div>
            <span style="position: relative; background: var(--bg-card); padding: 0 10px; color: var(--text-muted); font-size: 12px; font-weight: 500; letter-spacing: 0.5px;">HOẶC</span>
        </div>
        <a href="<?= $this->url('google-login') ?>" class="btn btn-outline w-100 d-flex align-items-center justify-content-center gap-2" style="height: 44px; font-weight: 600; border-color: rgba(255,255,255,0.15);">
            <img src="https://www.gstatic.com/images/branding/product/1x/gsa_512dp.png" width="18" height="18">
            Tiếp tục với Google
        </a>
    </form>
    <p class="login-footer-text">
        Đã có tài khoản?
        <a href="<?= $this->url('library/auth', ['action' => 'login']) ?>" class="login-footer-link">Đăng nhập ngay</a>
    </p>
    <div class="login-copyright">
        © 2026 Bản quyền thuộc về <span class="login-footer-link">Thư Viện HDPE</span>
    </div>
</div>

<script>
function togglePasswordVisibility(inputId, btnEl) {
    const input = document.getElementById(inputId);
    const icon = btnEl.querySelector('i');
    if (input.type === 'password') {
        input.type = 'text';
        icon.classList.remove('bi-eye');
        icon.classList.add('bi-eye-slash');
    } else {
        input.type = 'password';
        icon.classList.remove('bi-eye-slash');
        icon.classList.add('bi-eye');
    }
}

document.addEventListener('DOMContentLoaded', function() {
    const passwordInput = document.getElementById('register-password');
    const reqLength = document.getElementById('req-length');
    const reqUppercase = document.getElementById('req-uppercase');
    const reqSpecial = document.getElementById('req-special');
    const strengthBar = document.getElementById('strength-bar');
    const strengthLabel = document.getElementById('strength-label');

    if (passwordInput) {
        passwordInput.addEventListener('input', function() {
            const val = passwordInput.value;
            
            // Check requirements
            const hasLength = val.length >= 8;
            const hasUppercase = /[A-Z]/.test(val);
            const hasSpecial = /[^a-zA-Z0-9\s]/.test(val);
            
            // Update checklist items
            function updateReq(el, met) {
                const icon = el.querySelector('i');
                if (met) {
                    el.style.color = '#22c55e'; // green accent
                    icon.className = 'bi bi-check-circle-fill';
                } else {
                    el.style.color = 'var(--text-muted)';
                    icon.className = 'bi bi-circle';
                }
            }
            
            updateReq(reqLength, hasLength);
            updateReq(reqUppercase, hasUppercase);
            updateReq(reqSpecial, hasSpecial);
            
            // Calculate strength score (0 to 3)
            let score = 0;
            if (hasLength) score++;
            if (hasUppercase) score++;
            if (hasSpecial) score++;
            
            const hasLowercase = /[a-z]/.test(val);
            const hasNumber = /[0-9]/.test(val);
            let extra = 0;
            if (hasLowercase) extra++;
            if (hasNumber) extra++;
            
            if (val.length === 0) {
                strengthBar.style.width = '0%';
                strengthBar.style.background = '#ef4444';
                strengthLabel.textContent = 'Trống';
                strengthLabel.style.color = 'var(--text-muted)';
            } else if (score < 2) {
                strengthBar.style.width = '25%';
                strengthBar.style.background = '#ef4444';
                strengthLabel.textContent = 'Yếu';
                strengthLabel.style.color = '#ef4444';
            } else if (score === 2) {
                strengthBar.style.width = '60%';
                strengthBar.style.background = '#f59e0b';
                strengthLabel.textContent = 'Trung bình';
                strengthLabel.style.color = '#f59e0b';
            } else {
                // score === 3
                if (extra === 2) {
                    strengthBar.style.width = '100%';
                    strengthBar.style.background = '#22c55e';
                    strengthLabel.textContent = 'Rất mạnh';
                    strengthLabel.style.color = '#22c55e';
                } else {
                    strengthBar.style.width = '80%';
                    strengthBar.style.background = '#10b981';
                    strengthLabel.textContent = 'Mạnh';
                    strengthLabel.style.color = '#10b981';
                }
            }
        });
    }
});
</script>
