<?php
$isEdit = ($mode === 'edit');
$title = $isEdit ? 'Cập nhật tài khoản' : 'Tạo tài khoản mới';
$actionUrl = $isEdit
    ? $this->url('library/user', ['action' => 'edit', 'id' => $user->id])
    : $this->url('library/user', ['action' => 'add']);

$this->headTitle($title);
$this->layout('layout/library-layout');
?>
<div class="page-header">
    <div class="d-flex align-center gap-3">
        <a href="<?= $this->url('library/user') ?>" class="btn btn-outline btn-icon"><i class="bi bi-arrow-left"></i></a>
        <div>
            <div class="page-title"><?= $title ?></div>
            <div class="page-subtitle">Thiết lập thông tin định danh và phân quyền truy cập cho người dùng thư viện.</div>
        </div>
    </div>
</div>

<div class="user-form-container" style="max-width: 100%;">
    <div class="card">
        <div class="card-body">
            <form method="POST" action="<?= $actionUrl ?>">
                <?= $this->formHidden($form->get('id')) ?>

                <div class="user-form-grid-2-1">
                    <div class="form-group user-form-group-compact">
                        <label class="form-label">Họ và tên *</label>
                        <input type="text" name="full_name" class="form-control"
                               value="<?= htmlspecialchars((string) ($form->get('full_name')->getValue() ?? '')) ?>"
                               maxlength="100" required>
                        <?php $messages = $form->get('full_name')->getMessages(); if ($messages) : ?>
                            <div class="user-form-error"><?= htmlspecialchars(implode(', ', $messages)) ?></div>
                        <?php endif; ?>
                    </div>
                    <div class="form-group user-form-group-compact">
                        <label class="form-label">Vai trò *</label>
                        <?= $this->formSelect($form->get('role')->setAttribute('class', 'form-select')) ?>
                        <?php $messages = $form->get('role')->getMessages(); if ($messages) : ?>
                            <div class="user-form-error"><?= htmlspecialchars(implode(', ', $messages)) ?></div>
                        <?php endif; ?>
                    </div>
                </div>

                <div class="user-form-grid-1-1">
                    <div class="form-group user-form-group-compact">
                        <label class="form-label">Tên đăng nhập *</label>
                        <input type="text" name="username" class="form-control"
                               value="<?= htmlspecialchars((string) ($form->get('username')->getValue() ?? '')) ?>"
                               maxlength="50" required>
                        <?php $messages = $form->get('username')->getMessages(); if ($messages) : ?>
                            <div class="user-form-error"><?= htmlspecialchars(implode(', ', $messages)) ?></div>
                        <?php endif; ?>
                    </div>
                    <div class="form-group user-form-group-compact">
                        <label class="form-label">Email *</label>
                        <input type="email" name="email" class="form-control"
                               value="<?= htmlspecialchars((string) ($form->get('email')->getValue() ?? '')) ?>"
                               maxlength="100" required>
                        <?php $messages = $form->get('email')->getMessages(); if ($messages) : ?>
                            <div class="user-form-error"><?= htmlspecialchars(implode(', ', $messages)) ?></div>
                        <?php endif; ?>
                    </div>
                </div>

                <div class="user-form-policy-card">
                    <div class="user-form-policy-title"><i class="bi bi-shield-check user-form-policy-icon"></i>Chính sách an toàn</div>
                    Mật khẩu tối thiểu 8 ký tự. Yêu cầu chứa ít nhất một ký tự in hoa và một ký tự đặc biệt để đảm bảo an toàn.
                </div>

                <div class="user-form-grid-1-1-margin-20">
                    <div class="form-group user-form-group-compact">
                        <label class="form-label"><?= $isEdit ? 'Mật khẩu mới' : 'Mật khẩu *' ?></label>
                        <div style="position: relative;">
                            <input type="password" name="password" id="user-password" class="form-control" <?= $isEdit ? '' : 'required' ?> style="padding-right: 40px;">
                            <button type="button" onclick="togglePasswordVisibility('user-password', this)" style="position: absolute; right: 12px; top: 50%; transform: translateY(-50%); background: none; border: none; color: var(--text-muted); cursor: pointer; padding: 0; display: flex; align-items: center;">
                                <i class="bi bi-eye"></i>
                            </button>
                        </div>
                        <?php if ($isEdit) : ?>
                            <div class="user-form-password-hint">Bỏ trống nếu giữ nguyên mật khẩu cũ.</div>
                        <?php endif; ?>
                        <?php $messages = $form->get('password')->getMessages(); if ($messages) : ?>
                            <div class="user-form-error"><?= htmlspecialchars(implode(', ', $messages)) ?></div>
                        <?php endif; ?>
                    </div>
                    <div class="form-group user-form-group-compact">
                        <label class="form-label"><?= $isEdit ? 'Xác nhận mật khẩu mới' : 'Xác nhận mật khẩu *' ?></label>
                        <div style="position: relative;">
                            <input type="password" name="password_confirm" id="user-password-confirm" class="form-control" <?= $isEdit ? '' : 'required' ?> style="padding-right: 40px;">
                            <button type="button" onclick="togglePasswordVisibility('user-password-confirm', this)" style="position: absolute; right: 12px; top: 50%; transform: translateY(-50%); background: none; border: none; color: var(--text-muted); cursor: pointer; padding: 0; display: flex; align-items: center;">
                                <i class="bi bi-eye"></i>
                            </button>
                        </div>
                        <?php $messages = $form->get('password_confirm')->getMessages(); if ($messages) : ?>
                            <div class="user-form-error"><?= htmlspecialchars(implode(', ', $messages)) ?></div>
                        <?php endif; ?>
                    </div>
                </div>

                <?= $this->formHidden($form->get('csrf')) ?>
                <?php $messages = $form->get('csrf')->getMessages(); if ($messages) : ?>
                    <div class="user-form-error-csrf"><?= htmlspecialchars(implode(', ', $messages)) ?></div>
                <?php endif; ?>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-save"></i><?= $isEdit ? 'Lưu thay đổi' : 'Tạo tài khoản' ?>
                    </button>
                    <a href="<?= $this->url('library/user') ?>" class="btn btn-outline">Hủy</a>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
function togglePasswordVisibility(inputId, btnEl) {
    const input = document.getElementById(inputId);
    const icon = btnEl.querySelector('i');
    if (input.type === 'password') {
        input.type = 'text';
        icon.classList.remove('bi-eye');
        icon.classList.add('bi-eye-slash');
    } else {
        input.type = 'password';
        icon.classList.remove('bi-eye-slash');
        icon.classList.add('bi-eye');
    }
}
</script>
